version: '3.8'
services:
  mcp:
    build: .
    container_name: qiq-mcp
    restart: unless-stopped
    environment:
      - PORT=8080
      - MCP_PORT=8080
    ports:
      - "8080:8080"
    # If you have env keys add: env_file: .env
    # env_file:
    #   - .env
