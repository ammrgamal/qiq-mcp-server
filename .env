MCP_PORT=3001
MCP_HOST=0.0.0.0

# Optional explicit HTTP port (fallbacks to MCP_PORT)
HTTP_PORT=3001

# Typesense connection (set real values on server; do not commit secrets)
TYPESENSE_HOST=b7p0h5alwcoxe6qgp-1.a1.typesense.net
TYPESENSE_PROTOCOL=https
TYPESENSE_PORT=443

# Keys (prefer search-only)
TYPESENSE_SEARCH_ONLY_KEY=
#TYPESENSE_API_KEY=
#TYPESENSE_ADMIN_API_KEY=

# Collection and query settings
TYPESENSE_COLLECTION=quickitquote_products
TYPESENSE_QUERY_BY=objectID,mpn_normalized,vendor_mpn,sku,name,display_name,search_keywords,tags,synonyms,brand,brand_normalized,item_type,category,subcategory,function_desc