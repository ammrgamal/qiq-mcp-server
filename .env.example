# MCP Server
PORT=8080
MCP_TOKEN=replace-with-uuid-token

# Typesense (optional; tools fall back to mock data if not set)
# Prefer search-only key in production. The server will pick the first non-empty among
# TYPESENSE_SEARCH_ONLY_KEY, TYPESENSE_API_KEY, TYPESENSE_ADMIN_API_KEY.
TYPESENSE_HOST=b7p0h5alwcoxe6qgp-1.a1.typesense.net
TYPESENSE_PROTOCOL=https
TYPESENSE_PORT=443
# Keys: set ONE of these (first non-empty will be used)
TYPESENSE_SEARCH_ONLY_KEY=7e7izXzNPboi42IaKNl63MTWR7ps7ROo
#TYPESENSE_API_KEY=vNoV3wWNOIcchRdJ1mO3payOT79e6jym
#TYPESENSE_ADMIN_API_KEY=dJEKGlMGZqsHlSGGxJn5fzGG99xMIxLH

# Collection and query settings
TYPESENSE_COLLECTION=quickitquote_products
# Comma-separated list of string fields to search by. Use fields confirmed in schema.
# For keyword search; object_id retrieval uses filter_by and does not depend on this list.
TYPESENSE_QUERY_BY=name,brand,category
