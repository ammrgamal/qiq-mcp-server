# MCP Server
PORT=8080
MCP_TOKEN=replace-with-a-strong-token

# Typesense (optional; tools fall back to mock data if not set)
TYPESENSE_HOST=your-typesense-host
TYPESENSE_PORT=8108
TYPESENSE_PROTOCOL=http
# Prefer search-only key in production
TYPESENSE_SEARCH_ONLY_KEY=your-search-only-key
# Fallback key (admin or general API key)
TYPESENSE_API_KEY=your-admin-api-key
TYPESENSE_COLLECTION=products
